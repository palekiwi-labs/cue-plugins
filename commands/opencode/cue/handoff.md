---
description: Evaluate state of work and prepare for handoff
agent: cue-evaluator
subtask: true
---

!`$HOME/.config/opencode/scripts/git-context.sh`

Perform a final audit of this session.
1. Evaluate if my logs and active executive plan are in sync with my code changes.
2. Ask me about any discoveries, dead-ends, or subagent consultations that I should record in the log before I leave.
3. Check if any out-of-scope items discovered during this session need to be captured as a pinned `todo` artifact.
4. Once satisfied, provide a "Handoff Report" summarizing the stopping point, the state of the master plan (`plan/index.md`), and critical context for the next agent.
