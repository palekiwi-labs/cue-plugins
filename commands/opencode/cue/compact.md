---
description: Summarize and compact the cueory log
agent: plan
---

!`$HOME/.config/opencode/scripts/git-context.sh`

1. Load the `cue` skill.
2. Read the entire `spec/log.md`.
3. Analyze the log for repetitive information, resolved issues that are no longer relevant, or patterns that can be summarized.
4. Rewrite the log (using `edit` on `spec/log.md`) to be more concise while preserving all critical historical decisions and discoveries.
5. Ensure the "Intent" in `spec/index.md` and the master plan at `plan/index.md` are still aligned with the compacted log.
